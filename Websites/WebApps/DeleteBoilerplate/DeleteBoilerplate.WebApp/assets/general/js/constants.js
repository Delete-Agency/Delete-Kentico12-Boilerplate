import { viewports, containerMaxWidth, defaultTransitionDuration } from '../../config';

const CONTAINER_MAX_WIDTH = containerMaxWidth;
const DEFAULT_TRANSITION_DURATION = defaultTransitionDuration;
const VIEWPORT_WIDTH_DESKTOP = viewports.desktop;
const VIEWPORT_WIDTH_TABLET = viewports.tablet;
const VIEWPORT_WIDTH_MOBILE = viewports.mobile;

export default {
    CONTAINER_MAX_WIDTH,
    DEFAULT_TRANSITION_DURATION,
    VIEWPORT_WIDTH_DESKTOP,
    VIEWPORT_WIDTH_TABLET,
    VIEWPORT_WIDTH_MOBILE,
};
